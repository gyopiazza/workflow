module.exports = {

    // The root of your project (relative to the gruntfile)
    root:   'project/',

    // Assets paths (relative to the root)
    css:    'assets/css/',
    js:     'assets/js/',
    img:    'assets/img/',

    // Other paths (relative to the gruntfile)
    tests:  'tests/',
    tasks:  'grunt/',
    build:  'build/',

};
